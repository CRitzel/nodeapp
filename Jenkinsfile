pipeline {
    agent any
    stages {
        stage('Exemplo') {
            steps {
                sh 'users'
                echo 'Hello World!'
                sh 'echo customVar = $customVar'
            }
        }
    }
}
